# Generated by Next.js during `npm run build` — it owns the block between its
# BEGIN/END markers and rewrites the file, space-only lines and missing final
# newline included. Formatting it just means the next build undoes the work and
# `npm run prettier:check`, the only automated check in this repo, fails again.
AGENTS.md
